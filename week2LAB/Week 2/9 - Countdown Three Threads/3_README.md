# Countdown — Three Threads

Same as the synchronized version but with three threads instead of two. The `synchronized "block" handles it just fine—it doesn't matter how many threads are competing; only one gets in at a time. Each thread has a different color so you can tell them apart.
